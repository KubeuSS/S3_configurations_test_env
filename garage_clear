#!/bin/bash
set -e
while read -r NAME IP; do
    multipass exec "$NAME" -- bash -c "
        pkill -9 garage 2>/dev/null || true
        rm -rf /home/ubuntu/garage-meta /home/ubuntu/garage-data /home/ubuntu/garage.toml
    " < /dev/null &
done < nodes_ips.txt
wait
